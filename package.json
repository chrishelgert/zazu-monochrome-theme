{
  "name": "zazu-snazzy-theme",
  "version": "1.0.0",
  "description": "A hyper-snazzy inspired theme for Zazu.",
  "main": "dist/main.css",
  "scripts": {
    "build": "postcss -u postcss-cssnext -o dist/main.css -m dist/main.css.map src/main.css",
    "lint": "stylelint src/*.css",
    "precommit": "npm run build && npm run build"
  },
  "author": "Chris Helgert",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "husky": "^0.13.1",
    "postcss-cssnext": "^2.9.0",
    "stylelint": "^7.8.0",
    "stylelint-config-standard": "^16.0.0"
  }
}
